// Задача "Сортировать в порядке по убыванию" 

'use strict';
alert ("Задача \"Сортировать в порядке по убыванию\" ");

let digitsArray = [5, 2, 1, -10, 8, 0];

digitsArray.sort((a, b) => b - a);

alert(digitsArray); 